#include <stdio.h>

int main() {
    int length;
    int width;
    printf("this program will calculate the area of rectangle\n");
    printf("Enter length\n");
    scanf("%d", &length);
    printf("Enter width\n");
    scanf("%d", &width);

    printf("Area of a Rectangle is %d\n", length*width);
}
